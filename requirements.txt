# Core
fastapi==0.135.2
uvicorn[standard]==0.46.0
PyYAML==6.0.3

# HTTP
aiohttp==3.14.1

# Database
aiosqlite==0.22.1
asyncpg>=0.29.0

# Monitoring
prometheus-client==0.25.0

# Security & Identity
cryptography==48.0.1
PyJWT==2.13.0

# Export & Observability
aiofiles==25.1.0
sentry-sdk[fastapi]==2.61.1

# Observability
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-instrumentation>=0.46b0
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-instrumentation-aiohttp-client>=0.63b1
opentelemetry-instrumentation-aiohttp-server>=0.63b1
opentelemetry-exporter-otlp>=1.25.0

# Utilities
cachetools==7.1.4

# Type stubs
types-PyYAML>=6.0.0
types-cachetools>=5.0.0
types-aiofiles>=24.1.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Optional: Secrets manager (falls back to env vars without this)
# infisical-sdk>=3.0.0

# Optional: NLP-powered PII detection (Presidio — replaced by ONNX masker below)
# presidio-analyzer
# presidio-anonymizer

# ONNX PII masking — OpenAI Privacy Filter NER (llmproxy-extended)
# Model download: huggingface-cli download openai/privacy-filter
# Uses `tokenizers` (Rust) directly — does NOT require `transformers` or `torch`.
onnxruntime>=1.18.0
numpy>=1.24.0
huggingface-hub>=0.20.0
tokenizers>=0.19.0

# Optional: GPU acceleration on AMD/Intel iGPU (Windows only, int8 variant only)
# onnxruntime-directml

# Optional: Context compression — 60-95% token reduction (llmproxy-extended)
# pip install "headroom-ai[ml,code]"
# headroom-ai[ml,code]

# Optional: WASM plugin support
# extism

# Optional: Parquet export
# pyarrow

# Distributed HA rate limiting & circuit breakers
redis>=5.0.0
